# BIN-825: auto-generated by setup-chaos-env.sh — never commit.
# Contents are safe dummy values, but pinning them in git would
# tempt someone to swap in real secrets next to the dummies.
.env.chaos
