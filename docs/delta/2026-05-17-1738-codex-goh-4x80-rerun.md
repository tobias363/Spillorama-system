# Delta-rapport — Codex / codex-goh-4x80-rerun-2026-05-17

## Hva ble endret

- `apps/backend/src/game/Game1TicketPurchaseService.ts` — bevarer transient wallet-feilkode ved purchase-failure, slik at 4x80-runneren kan skille retrybare wallet-contention fra ekte kjøpsfeil.
- `apps/backend/src/game/Game1ScheduledTicketMarkService.ts` — validerer scheduled ticket-mark mot eksplisitt `scheduledGameId`, også når room-binding er ryddet etter round reset.
- `apps/backend/src/sockets/game1ScheduledEvents.ts` — scheduled join-path har rask ack, canonical room recovery ved manglende BingoEngine-rom, og reattach-invariant fra FRAGILITY F-05.
- `apps/backend/src/sockets/gameEvents/ticketEvents.ts` — `ticket:mark` sender eksplisitt scheduledGameId inn i DB-backed scheduled-validator.
- `packages/game-client/src/games/game1/Game1Controller.ts` og `packages/game-client/src/net/SpilloramaSocket.ts` — klient sender scheduledGameId på mark-ack og tåler scheduled reconnect/retry-path bedre.
- `scripts/dev/goh-full-plan-run.mjs` — 4x80 GoH-runner fikk retry/rehydration for transient wallet, join og mark-ack, pluss korrekt `--output <path>` parsing.
- `docs/operations/CODEX_CLAUDE_WORKTREE_ROUTINE.md` — permanent Codex/Claude worktree-rutine med fresh-main, lock-file og PR-body-regler.
- `docs/evidence/20260517-*` — preflight/postfail/postpass Sentry/PostHog/DB/pilot-monitor snapshots og GoH full-plan run reports.

## Hva andre steder kunne ha blitt brutt

- Scheduled Spill 1 room-join kan brekke re-entry hvis reattach-guard fjernes eller flyttes etter `joinRoom`.
- Legacy non-scheduled `ticket:mark` kan brekke hvis scheduledGameId-logikk lekker inn i vanlig BingoEngine-path.
- Wallet purchase kan dobbel-kompensere eller maskere reelle feil hvis transient retry kun baseres på generiske exceptions.
- Game-client reconnect kan miste mark-state hvis scheduledGameId ikke overlever fra active ticket/session state.
- GoH-runner evidence kan bli misvisende hvis output parsing ikke skiller raw JSON, markdown report og recovered summary.
- Codex/Claude parallellarbeid kan gi dokumentasjonskonflikt hvis begge endrer `PITFALLS_LOG.md`, `AGENT_EXECUTION_LOG.md`, workflows eller `pm-orchestration-pattern` uten rebase.

## Nye fragilities oppdaget

- Ingen ny `FRAGILITY_LOG.md` entry ble lagt til. Eksisterende F-05 gjelder fortsatt for reattach-guard i scheduled join-paths.
- Ny documented pitfall: `PITFALLS_LOG.md` §6.25 om CLI-output parsing og recovered evidence summary.
- Oppdatert documented pitfall: `PITFALLS_LOG.md` §4.9/§6.22 for scheduled join/mark/reconnect under 4x80 GoH-load.
- Oppdatert wallet pitfall: `PITFALLS_LOG.md` §2.12 for transient wallet contention som skal retryes, ikke klassifiseres som permanent purchase failure.

## Brief for neste agent

- Før du rører scheduled room join eller reconnect, les `FRAGILITY_LOG.md` F-05 og behold pattern: finn eksisterende player via wallet, reattach socket, og kall `joinRoom` bare for helt ny player.
- Før du rører scheduled `ticket:mark`, behold eksplisitt `scheduledGameId` i wire-contract og validator-call. Room binding kan være ryddet etter round reset.
- Før du endrer purchase retry i runneren, skill transient wallet contention fra irreversible wallet/compliance-feil. Ikke retry ukritisk rundt pengeflyt.
- Før Codex og Claude jobber parallelt, følg `docs/operations/CODEX_CLAUDE_WORKTREE_ROUTINE.md`: separate worktrees, fresh `origin/main`, én eier for shared files.
- Hvis ny 4x80-test feiler, lagre preflight/postfail evidence før fix og postpass evidence etter fix. Ikke overskriv tidligere evidence.

## Tester kjørt

- `git diff --check origin/main...HEAD` — pass.
- `node --check scripts/dev/goh-full-plan-run.mjs` — pass.
- `npm exec -- tsx --test src/game/Game1TicketPurchaseService.test.ts src/game/Game1ScheduledTicketMarkService.test.ts src/sockets/gameEvents/ticketEvents.scheduled.test.ts src/sockets/__tests__/game1JoinScheduled.test.ts src/sockets/__tests__/game1ScheduledEvents.reconnect.test.ts` fra `apps/backend` — pass, 55/55.
- `npm --prefix apps/backend run check` — pass.
- `npm -w @spillorama/shared-types run test -- --test-name-pattern='TicketMarkPayload|wire-contract'` — pass, 131/131.
- `npm -w @spillorama/game-client run check` — pass.
- `node scripts/dev/goh-full-plan-run.mjs --players-per-hall=80 --all-plan-positions` med Sentry/PostHog/DB/pilot-monitor snapshots — pass, 13/13 planposisjoner, 320 spillere, 4160 purchases, 159418 mark acks, 0 mark failures, 0 pilot-monitor P0/P1, 0 nye/increased Sentry issues.
