{
  "name": "@spillorama/admin-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:prod-backend": "VITE_DEV_BACKEND_URL=https://spillorama-system.onrender.com vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^8.0.12",
    "vitest": "^4.1.6",
    "@vitest/ui": "^4.1.6",
    "jsdom": "^24.0.0",
    "@types/node": "^20.12.0"
  }
}
